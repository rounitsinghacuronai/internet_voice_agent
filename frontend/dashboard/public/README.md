# Static assets

Drop your brand logo here as **`logo.png`** (or `logo.svg`) and it will
automatically appear in the sidebar and on the login screen — no code changes
needed. Until a file exists, an on-brand fallback mark is shown.

Recommended: a square PNG, ~256×256, transparent or white background.
